import { FontupdateDirective } from './fontupdate.directive';

describe('FontupdateDirective', () => {
  it('should create an instance', () => {
    const directive = new FontupdateDirective();
    expect(directive).toBeTruthy();
  });
});
